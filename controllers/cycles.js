var mongoose = require( 'mongoose' );
var Cycle = mongoose.model( 'Cycle' );
var Water = mongoose.model( 'Water' );
var Response = require( __dirname + '/../lib/Response' );

module.exports = {
	new: function( req, res ){
		
	}
}
